{
    "name": "transcript-notes-server",
    "version": "1.0.0",
    "description": "Backend server for Transcript Notes application",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "install-deps": "npm install && cd react-transcript-notes && npm install --legacy-peer-deps",
        "build": "cd react-transcript-notes && npm run build"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1",
        "axios": "^1.6.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}